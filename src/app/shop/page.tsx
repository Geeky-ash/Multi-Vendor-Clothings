// This page will eventually have more advanced filtering and a full list of shops.
// For now, we can reuse the Home component's content.
import Home from "../page";

export default function AllShopsPage() {
  return <Home />;
}